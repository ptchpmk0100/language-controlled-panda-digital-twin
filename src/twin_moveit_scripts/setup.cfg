# Without this, setuptools installs console scripts into bin/ and neither
# `ros2 run` nor a launch file can find them. ros2 pkg create generates this
# file; a package assembled by hand is the one that forgets it.
[develop]
script_dir=$base/lib/twin_moveit_scripts
[install]
install_scripts=$base/lib/twin_moveit_scripts
